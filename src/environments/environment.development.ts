// Development environment (used by `ng serve`). Calls go through the dev proxy
// (frontend/proxy.conf.json) to the local backend on http://localhost:3000.
export const environment = {
  production: false,
  apiBase: '/api',
};
